<resources xmlns:tools="http://schemas.android.com/tools">

    <style name="Theme.MiniProject7" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <item name="colorPrimary">@color/green_mid</item>
        <item name="colorPrimaryVariant">@color/green_dark</item>
        <item name="colorOnPrimary">@android:color/white</item>
        <item name="android:statusBarColor">@android:color/transparent</item>

        <!-- 🔥 Fix: Disable Material tint on buttons -->
        <item name="materialButtonStyle">@style/CustomMaterialButton</item>
    </style>

    <style name="CustomMaterialButton" parent="Widget.MaterialComponents.Button">
        <item name="backgroundTint">@null</item>
    </style>

</resources>
